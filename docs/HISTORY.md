# 변경 이력 아카이브

CLAUDE.md의 변경 이력 표에서 옮겨온 과거 행. CLAUDE.md에는 최근 5행만 유지하고, 넘치는 행은 이 파일의 표 **맨 위에** 추가한다.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-08 | **노24(공연 예매)·먼바다투어(여행 예약) 신설 + 가로 띠 배너를 이동용으로** — 여행사 배너 오른쪽에 노24가 나란히 서고, 둘 다 누르면 사이트로 간다 | shows·trips(신규 data), TicketSite·TravelSite(tsx+css 신규), activities(`concert`·`travel`), sites(`render: 'ticket'|'trip'` + 2종), banners(`siteId` + 노24 배너), NeverPortal(배너 이동 + 띠 2열), BrowserApp, banners.test(7건)·sites.test | 사용자 지시. ⚠️ **광고와 이동을 겸하지 않는다** — 한 배너가 돈도 주고 이동도 하면 "이 배너는 무엇인가"가 두 가지가 되고 하루 한 번이라는 보상 상한이 이동 때문에 소모된다(`reward`와 `siteId`는 배타, 테스트가 지킨다). 옆 배너존은 그대로 광고다. ⚠️ **두 사이트 다 시집이와 같은 부류다**: 고르는 것은 무엇을 보러/어디로 가는가뿐이고 **값은 활동이 갖는다**(공연 60,000 / 여행 250,000) — 상품마다 가격을 달면 밸런스 테스트가 못 보는 두 번째 출처가 생긴다. ⚠️ **여행을 여러 턴짜리로 만들지 않았다** — 며칠짜리 활동은 예약·정산·번아웃에 전부 새 규칙을 요구한다(구독을 뺀 것과 같은 판단). "3박 5일"은 표시 전용이고 감각은 행동력 -30과 금액이 진다. ⚠️ **입구가 배너 하나뿐이라 배너와 사이트의 색을 맞췄다**(노24 인디고 / 먼바다투어 심해 남색) — 색 축이 이미 포화라 "모든 사이트는 색이 다르다"보다 이 규칙이 낫다. DB 하늘색을 그대로 쓰면 알바몬과 겹친다. 실측 대비 5.4~16:1 |
| 2026-08-08 | **브라우저 다중 탭** — 사이트를 열면 탭이 붙고 탭의 ✕로 닫는다. [+] 새 탭 버튼 부활 | browserTabs(신규 systems+test 10건), BrowserApp(tsx 탭 줄·이동 통로 분리), BrowserApp.css(활성 탭 색·[+] 글리프) | 사용자 지시. **규칙을 컴포넌트 밖에 뒀다**(`systems/browserTabs.ts`) — 열기·전환·닫기·되돌리기가 서로 얽혀서 `useState` 콜백 안에 흩어지면 화면을 열어 보지 않고는 확인할 수가 없다(이력 `browserHistory.ts`와 같은 층). ⚠️ **가장 중요한 판단: 이동 통로를 둘로 갈랐다.** 링크(포털 카드·즐겨찾기)는 **새 탭**, 주소창·[홈으로]는 **현재 탭**이다 — 모든 이동이 새 탭이면 탭마다 이력이 한 칸뿐이라 **뒤로/앞으로가 영영 비활성인 죽은 컨트롤이 된다**(실제 브라우저의 갈래와 같고 `browserTabs.test.ts`가 그 사실을 단언한다). ⚠️ **같은 사이트의 탭을 둘 만들지 않는다** — 즐겨찾기를 두 번 누르면 탭이 둘이 되고 그때부터 "지금 보는 게 어느 탭인가"를 사람이 세야 한다. ⚠️ **마지막 탭을 닫으면 창이 닫힌다**(`closeTab`이 `null`을 돌려주는 것이 그 뜻 — 빈 탭 줄만 남은 브라우저를 만들지 않는다), 활성 탭을 닫으면 오른쪽(없으면 왼쪽)으로 간다. ⚠️ **탭 하나에 버튼이 둘이라 `<button>`을 겹칠 수 없다** — 바깥은 `role="tab"` div이고 제목 쪽이 전환, ✕가 닫기다. **활성 탭만 렌더한다**(전부 마운트하면 포털 실검 타이머가 탭 수만큼 돈다 — 대신 탭 전환 시 사이트 필터·검색어가 초기화된다). [+]는 예전에 "눌러도 아무 일 없는 버튼"이라 뺐던 것인데 이제 진짜로 동작하므로 되살렸다. ⚠️ `--os-hover-alt`를 쓸 뻔했다가 **정의되지 않은 토큰**이라 `--os-border-strong`으로 고쳤다(없는 var는 선언 전체를 죽여 화면에서만 조용히 깨진다 — 이 리포에서 이미 한 번 터진 사고다). ⚠️ 실측: 1024px에서 탭 6개 + [+]가 넘침 0·제목 잘림 0, 중복 열기 방지·탭 전환·✕ 닫기·마지막 탭에서 창 닫힘까지 전부 확인 |
| 2026-08-08 | **메신저 첫 판을 비웠다** — 카톡은 오픈채팅 둘만, 너아무튼온은 취직해야 시작된다 | messages(`Thread.requires`·`requiresEmployment`), systems/messages(`threadVisible`·`visibleThreadsOf`·`channelVisible`), ChatApp(빈 목록 안내), ToastHost·gameStore(알림 필터), desktopItems(`requiresEmployment` + 너아무튼온을 프로그램 열 맨 뒤로), types, Desktop·MobileShell, messages.test(7건) | 사용자 지시. 아는 사람 방을 처음부터 깔아 두면 **"혼자 시작해서 사람을 만들어 간다"가 시작부터 완결돼 있다** — 친화력 4·8·16에 가족·민지·동아리가 하나씩 닿는다(축을 하나로 묶은 이유는 한 문장으로 읽혀야 해서다). ⚠️ **오픈채팅에는 조건을 안 걸었다** — 헬스장·미용실이 초반 선택지라 잠그면 시작하자마자 갈 곳이 하나 준다. ⚠️ **판정을 `threadVisible` 하나로 모은 것이 핵심**이다: 목록만 걸렀으면 편성표 0턴의 민지 메시지가 **열 수 없는 토스트**로 떴다(토스트·자동 진행 요약도 같은 필터를 지난다). ⚠️ 너아무튼온은 **앱 아이콘까지 숨긴다** — 앱만 두면 빈 목록이 뜬다. 조건부 항목이라 클립스튜디오처럼 **프로그램 열 맨 뒤로 옮겼다**(가운데 두면 취직 전까지 칸이 뚫린다). ⚠️ 빈 목록이 이제 **첫 판의 기본 화면**이라 "대화방이 없습니다" 한 줄로는 고장으로 읽힌다 — 무엇을 하면 생기는지까지 적었다 |
| 2026-08-08 | **클립스튜디오 계열 신설** — 하이마루 타블렛 2종 → 바탕화면에 클립스튜디오·갤러리가 생기고, 그릴 때마다 등급이 매겨진 그림이 쌓이며, 트위터에 올리면 팔로워가 늘고 **주 1회 팔로워 비례 정산**이 밤에 들어온다 | types(스탯 `art`·`Artwork`·`TwitterState`·`FolderId` 'gallery'·`requiresItem` 배열·`producesArt`·`DesktopItem.requiresItem`), artworks·artwork·twitter(신규 data+systems+test 25건), items(타블렛 2종 + `requiredItemIds`/`Label`/`Stores`), activities(`draw`), turn(`ownsRequired`·`stampArtwork`·`nightPayoutPending` 4번째 원천), desktopItems(조건부 항목 + `desktopEntries(_, ownedIds)`), Desktop·MobileShell·ExplorerApp·TwitterSite(+css)·SchedulerApp·activityPreview·gameStore(`postArtwork`·`reviveTwitter`·`advanceTwitter`), statMeta, 테스트 6종 갱신 | 사용자 지시. **`draw`는 이 게임에서 유일하게 아이템을 만들어 내는 활동이다** — 그래서 쌓인 것이 나중에 돈이 된다(스탯만 올리는 활동과 성격이 다르다). ⚠️ **타블렛 둘이 같은 문을 연다**(`requiresItem` 배열 = OR): 장비별로 활동을 쪼개면 바탕화면에 클립스튜디오 아이콘이 둘 생기므로, 갈리는 것은 **등급 보너스뿐**이다(액정 +0.12 — 1,150,000원의 값어치가 이 한 줄이다). ⚠️ **등급을 저장하지 않고 그릴 때의 사실만 박는다**(예술·창의력·장비): 저장하면 기준을 손볼 때 옛 그림이 낡고, 안 박으면 스탯이 오를 때마다 옛 그림이 저절로 명작이 된다. 등급 척도는 **스탯과 같은 `StatRank`**를 재사용했다("A등급 그림"과 "예술 A"가 나란히 뜨는데 척도가 갈리면 못 읽는다). ⚠️ **기준을 상한 999가 아니라 `ART_MASTERY` 300으로 잡았다** — 999면 S에 닿는 데 그리기 60회가 넘어 100일 판에서 아무도 못 본다. ⚠️ **`FOLLOWER_CAP`(120,000)이 "판은 반드시 끝난다"를 지탱한다**: 상한 수입 34,285원/일 < 가장 싼 집의 마지막 물가 생활비 45,600원/일이고 `twitter.test.ts`가 데이터에서 직접 계산해 지킨다(규칙을 뒤집는 증명까지 붙였다 — 급여가 물가 배율을 안 타는 것과 같은 장치). ⚠️ 정산은 `nightPayoutPending`의 **네 번째 원천**이고 보는 것은 금액이 아니라 커서 `paidDay`다(정기예금 만기와 같은 형태). ⚠️ **바탕화면 조건부 항목은 `DESKTOP_ITEMS`에서 빼지 않고 `desktopEntries`가 거른다**(기본 격자가 배열에서 파생된다)이고 **자기 열 맨 뒤**여야 한다 — 가운데면 살 때까지 빈 칸이 남는다. ⚠️ 컴파일러가 잡아 준 것: `requiresItem`을 배열로 넓히자 호출부 여섯이 터졌다(스케줄러·잠금 사유·테스트 셋) — 정규화를 `requiredItemIds` 한 곳으로 모았다 |
| 2026-08-08 | **네이놈 뉴스 아래 가로 띠 배너** 신설 — 여행사 광고 1건 | banners(`Banner.placement`·`bannersFor` + 먼바다투어), NeverPortal(`BannerButton` 부품 추출 + 띠 구역, css) | 사용자 지시. **세로 배너존과 같은 부품(`BannerButton`)을 쓴다** — 자리마다 그리면 보상 문구(`AD +100원`/`오늘 받음`/`공지`)가 반드시 한쪽만 낡는다. ⚠️ **보상 경로를 새로 만들지 않았다**: 자리가 늘어도 하루 한 번 100원이라 여기서 받으면 옆 배너들이 함께 잠긴다(`canClaimAdBonus` 단일 판정 — 자리 수만큼 벌 수 있으면 상한이 뜻을 잃는다). ⚠️ **위가 아니라 아래다** — 위에 눕히면 첫 화면이 광고로 채워져 "뉴스가 주인공"이 깨진다(기존 `nv-columns` 주석의 규칙 그대로). 자리 축은 `placement` 하나이고 목록은 `bannersFor`가 파생한다(`buyableFor`와 같은 규칙 — 자리별 배열 금지). ⚠️ 실측이 잡은 것: **`<button>`이 블록 컨테이너 안에서 내용 폭으로 줄어들어** 띠가 뉴스 단보다 119px 좁았다(세로 배너존은 flex-column의 stretch가 채워 주고 있었다 — 같은 부품인데 이 자리에서만 폭이 무너졌다). `width: 100%`로 고쳐 952px 일치. ⚠️ 그라데이션은 **모든 정지점 위에서 흰 글자가 서야** 한다(가로 배너는 글자가 띠 전체에 눕는다) — 처음 고른 민트(#6fc7b6)는 1.98:1이라 버렸고, 지금은 합성 픽셀 실측으로 11.6 / 6.0 / 6.0이다 |
| 2026-08-08 | **무진장(의류 쇼핑몰) 신설 + TPO 보너스** — 옷을 가지고 있으면 때와 장소가 맞는 활동의 성장 상승분이 20%(최소 +1) 커진다 | items(`outfit.fits`·`OUTFIT_BONUS`·`outfitsFor`·의류 4종·`ItemStore`에 `'wear'`), turn(`outfitFor`·`outfitBonusFor`·`applyEffects`+`outfitBoost`), activityPreview(같은 계산 + `outfit` 노출), sites(`render: 'wear'`·`STORE_SITES`), WearSite(tsx+css 신규), BrowserApp, outfit.test(13건), sites.test·delivery.test(가게 셋으로 확장) | 사용자 지시("운동복을 가지고 헬스장에 가면 상승치가 소량 상승"). ⚠️ **지속 효과가 아니다** — 밤 정산을 건드리지 않고 **활동을 실행하는 순간에만** 곱해지므로 "지속 효과를 만들지 않는다"(전자기기 규칙)는 그대로다. ⚠️ **돈·행동력·멘탈에는 안 붙인다**: 돈에 붙이면 옷 한 벌이 경제를 흔들고(밸런스 시뮬레이션이 보는 축), 비용에 붙이면 "갖춰 입으면 덜 지친다"는 다른 규칙이 된다. 약속한 것은 **성장이 조금 잘 되는 것**뿐이다. ⚠️ **최소 +1**이 필요했다 — 면접(매력 2)·출근(친화력 1)은 20%가 반올림돼 0이 되어 정장을 사도 화면이 안 변한다(거짓말이 된다). ⚠️ **겹쳐 쌓지 않는다**(다 사면 배수가 되는 구조 금지)이고 **입고 벗는 상태를 만들지 않았다** — 가지고 있으면 적용이다(옷 고르기는 정답이 뻔해서 선택이 아니라 절차가 된다). 관계는 `ShopItem.outfit.fits` 한 곳에만 적고 화면은 `outfitsFor`로 뒤집어 찾는다. 색은 color DB `Wardrobe & Outfit Planner`(로즈+골드)이고 ⚠️ DB 금색(#D97706)은 흰 배경 3.2:1이라 **글자에는 #92400E**를 쓴다 |
| 2026-08-08 | **증기**(가짜 스팀) 신설 — 바탕화면 아이콘 → 어두운 라이브러리 창, 게임 6종을 켜서 시간을 보낸다 | steam(신규 data+systems+test), SteamApp(tsx+css 신규), types(`steam` 상태·`WindowKind`), gameStore(`playGame`), appForWindow(+테스트 ALL_KINDS), desktopItems, Window.css, MobileShell.css, shortcuts.test | 사용자 지시(레퍼런스: 실제 스팀 라이브러리). **게임이 수치를 갖지 않는 것**이 핵심 — 켜는 활동은 `game` 하나이고 라이브러리는 "무엇을 하며 시간을 보내는가"만 정한다(미디북스의 책·시집이의 영화와 같은 규칙). 게임마다 효과를 주면 "멘탈 회복처는 넷" 불변식이 라이브러리 크기만큼 갈라진다. 플레이 시간은 **횟수에서 파생**한다(둘 다 저장하면 어긋날 값이 둘이 된다). 색은 color DB `Gaming`의 구조에 레퍼런스 색상을 얹고 **합성 픽셀로 실측**했다(5.6~8.6:1). ⚠️ 실측이 잡은 것 셋: `dark`만 켜면 흰 캡션 글리프가 밝은 타이틀 바에서 사라진다(**`bareTitle`과 짝**이라야 한다) · `bareTitle` 창은 본문 패딩이 이미 0이라 음수 마진을 겹치면 가로 40px이 넘친다 · 프로그램 열이 꽉 차 바로 가기가 다음 열로 넘어가면서 `shortcuts.test`가 깨졌다(행 번호 대신 `firstFreeCell`로 기대값을 파생시켰다). ⚠️ **바탕화면 아이콘은 책장 → 퍼즐 조각으로 바꿨다**(사용자 지시): 게임기·컨트롤러 모양은 **다색 세트 `fluent-color` 890개에 없고** 단색 `mdi`에만 있는데, 바탕화면은 다색 규칙이라 그것만 회색 선으로 떠서 시스템 글리프로 읽힌다. 활동 `game`·스탯 `gaming`과 같은 그림이 되는 것은 오히려 규칙에 맞다 — 증기를 켜서 하는 일이 정확히 그 활동이다 |
| 2026-08-08 | **솔리테어(클론다이크) 구현** — 시작 메뉴의 마지막 미구현 항목. 규칙·섞기(순수) + 펠트 테이블 화면 | solitaire(신규 systems+test 15개), SolitaireApp(신규 tsx+css), appForWindow(`EXCLUDED_KINDS`에서 승격), Window.css | 사용자 지시. **육성 게임의 상태를 한 톨도 안 건드린다** — 시작 메뉴는 "게임 바깥의 도구"라는 규칙 그대로라 스탯·턴·돈이 안 움직이고, 그래서 밸런스·번아웃·엔딩 테스트에 손댈 것이 없다. **판은 컴포넌트의 `useState`에만 산다**(store도 세이브도 아니다 — 창을 닫으면 끝난다. 실제 윈도우 솔리테어와 같다). 섞기는 `Math.random`이 아니라 **시드를 받는 순수 함수**이고 시드는 화면이 정한다(복권과 같은 방식 — 그래서 "이 시드의 판은 이렇게 깔린다"를 테스트가 단언한다). **옮기는 길은 둘이다**: 끌어다 놓기(HTML5 drag) + 누르기("집고 → 놓기"). 처음엔 누르기만 만들었다가 사용자 지적("카드 드래그 이동 안됨?")으로 드래그를 얹었는데, **누르기를 지우지 않은 것**이 규칙이다 — 드래그는 터치에서 동작하지 않고 키보드로는 아예 불가능한데 카드가 전부 `<button>`이라 누르기만으로 끝까지 둘 수 있다(ux `gesture-alternative`). 둘 다 `applyMove` 하나를 지나므로 규칙이 갈릴 수 없고, 받아 줄 더미는 **끌기 시작할 때만** 밝힌다(상시 표시는 판 전체가 반짝여 소음이 된다). 놓을 수 없는 자리를 누르면 그 카드를 새로 집는다 — 아무 일도 안 일어나는 클릭을 만들지 않는다. 색은 `--sol-*`로 이 파일 안에 가뒀다(ui-ux-pro-max color `Card & Board Game`: 펠트 초록 + 금색, 흰 글자 7.0:1 / 빨강 무늬 4.8:1). ⚠️ 금색은 펠트 위 2.2:1이라 **글자·의미에 안 쓴다**. ⚠️ **카드 뒷면을 펠트와 같은 초록 빗금으로 만든 것이 실수였다**(사용자 지적: "왜 패산에 카드는 없어?" → "남은 카드가 아예 안 보임") — 초록 테이블 위의 초록 카드는 **카드가 아니라 빈 자리로 읽힌다.** 실제 카드처럼 **흰 테두리를 남기고 안쪽만 파랑 빗금**(`padding` + `content-box` 배경)으로 바꿨다: 흰 테두리는 펠트 위 7:1이라 배경이 뭐든 카드의 윤곽이 먼저 읽히고, 앞면(흰 카드)과 실루엣이 같아진다. 파랑은 뒷면 전용 색이라 앞면과 헷갈리지 않는다. ⚠️ 실측이 잡은 것: 카드 폭 88px을 박았더니 창 720px 안에서 **2px이 모자라 가로 스크롤**이 났다(창 720 = 테두리 2 + 본문 718 → 안쪽 686 < 필요 688) — 격자 `repeat(7, minmax(64px, 1fr))`로 바꿔 폭을 나누게 했고, 그 김에 윗줄(산·기초 더미)이 작업 더미 열과 정확히 맞아떨어진다 |
| 2026-08-08 | **미용실 오픈채팅** 신설 — 1회 방문(25,000원)과 정기권(150,000원 = 6회분)이 매력을 올린다 | activities(`salon-visit`·`salon-member`), items(`salon-pass`), messages(THREAD `salon` + 편성표 2줄), activities.test(4건) | 사용자 지시. **헬스장의 판을 그대로 되풀이한 것**이 핵심이다 — 1회권 활동 + `requiresItem`으로 잠긴 정기권 활동 + 오픈채팅 `offer` 두 칸(활동 실행 / 아이템 주문 + 주간 예약). 구조가 같아야 플레이어가 "오픈채팅에서 파는 것"을 한 번만 배운다. 다른 것은 올리는 스탯뿐이다(헬스장=체력 그릇 / 미용실=**매력**). ⚠️ **주간 예약 요일을 토요일로 갈랐다** — 헬스장(목)과 같은 요일이면 둘 다 끊은 사람의 예약이 매주 같은 슬롯에서 부딪혀 한쪽이 조용히 밀려난다. ⚠️ `social`(메신저, 매력 5 + 멘탈 8)과 성격도 갈랐다: 미용실은 멘탈을 거의 안 주는 대신 행동력이 싸다(같은 값이면 매력 올리는 길이 하나로 준다). 정기권 값 = 1회 6회분은 헬스장과 같은 규칙이고 테스트가 수식으로 지킨다 |
| 2026-08-08 | 알바몬을 **레퍼런스(실제 구인 사이트 홈) 판형으로** 다시 짰다 — 이벤트 띠 → 헤더 → **네비 줄**(=직종 필터) → 어두운 히어로(오늘 최고 일당) → **회색 구역 안의 4열 카드 격자** → 푸터 | AlbamonSite(tsx+css 전면) | 사용자 지시(레퍼런스=실제 알바몬 스크린샷). ⚠️ **색은 안 바꿨다** — 레퍼런스는 주황이지만 `TechSite.css`에 "파랑 셋·보라·코랄·청록·검정·골드가 찼고 **주황만 비어 있다**(설계자 확정)"고 못 박혀 있어 하이마루와 계열이 겹친다. 물어봤고 **하늘색 유지 + 판형만 교체**로 결정됐다. 그래서 **새 색을 한 개도 안 만들었다**: 페이지 배경을 흰색으로 내리고 기존 `--ab-bg`(#f0f9ff)를 구역 배경으로, `--ab-text`(#0c4a6e)를 히어로 배경으로 **자리만 옮겼다**(추가한 건 팔레트 secondary `--ab-accent` #0ea5e9 하나 — 히어로 배경 위 3.4:1이라 **32px 숫자에만** 쓴다). **레퍼런스에서 덜어낸 것**: 로그인/회원가입·이력서 등록(계정이 없다), 브랜드알바 캐러셀·추천 아이콘 줄(갈 데 없는 링크), 지역별 칩(공고 8건에 필터 축 둘은 과하다), 카드 [+] 스크랩, 구역 머리 [AD](광고는 배너존만 보상을 준다). 네비 줄 자리에는 **진짜 직종 필터**를, 히어로 자리에는 **오늘 최고 일당**을 앉혔다(장식 금지 — 아점 KEYWORD 격자와 같은 방식). ⚠️ 실측: 1440px에서 4열 268px·넘침 0·행별 카드 높이 균일, 900px 3열 / 780px 2열로 접힌다. 격자 최소 폭 **240px**은 계산값이다(5열이 되려면 211px여야 한다) |
| 2026-08-08 | 파일 탐색기(사진첩·아이템 인벤토리)를 **실제 윈도우 11 탐색기 판형으로** 다시 만들었다 — 빵부스러기 주소 줄 + 검색, 명령 모음(정렬·보기), 트리 탐색 창, 정렬되는 열 제목 4개(이름·수정한 날짜·유형·크기) | ExplorerApp(tsx+css 전면), MailApp.css·SchedulerApp.css(같은 버그 수정) | 사용자 지시(레퍼런스=실제 탐색기 스크린샷). ⚠️ **근본 원인은 따로 있었다**: `--os-line`·`--os-hover`가 **정의되지 않은 토큰**이라 테두리와 hover 선언이 통째로 죽어 있었다(CSS는 없는 var를 만나면 그 선언을 버린다 — 빌드·테스트는 통과하고 화면에서만 깨지는 그 사고). 같은 오타가 MailApp·SchedulerApp에도 있어 함께 고쳤다. **레퍼런스에서 덜어낸 것**: 탭 줄(창 타이틀이 이미 폴더 이름을 갖는다·[+]는 갈 데가 없다), 뒤로/앞으로/위로/새로 고침(폴더가 둘뿐이고 창 안에서 이동하지 않는다), 잘라내기·삭제 계열(인벤토리에서 물건을 지우는 건 UI가 아니라 규칙 변경이다). 남긴 것은 전부 동작한다. ⚠️ 실측이 잡은 것: 날짜·유형·크기를 128px씩 주면 **이름이 12자에서 잘렸고**(100/96/64로 좁혔다), 아이콘 격자에 `1fr`을 주면 타일이 창 폭에 맞춰 벌어져 격자가 아니라 줄로 보였다(고정 96px) |
| 2026-08-07 | **[아이콘 위치 초기화]를 바탕화면 왼쪽 아래 → 작업 표시줄 왼쪽 끝으로** 옮겼다(`.desktop-restore` 삭제) | Taskbar, Desktop(tsx+css), icons(`resetIcons`) | 사용자 지적("초기화 버튼 때문에 마지막 줄 아이콘 이름이 안 보여") — 포토샵·VS 코드가 늘면서 프로그램 열이 바닥까지 차자 버튼이 마지막 라벨을 덮었다. **자리를 옮긴 게 아니라 판을 옮긴 것**이 핵심이다: 아이콘은 위에서부터 채워지고 바로 가기까지 아래로 자라므로 **아이콘 판 안의 어떤 고정 자리도 언젠가는 가려진다**(왼쪽 위로 피해도 같은 일이 난다). 왼쪽 끝은 설계자 지시이고, 그 칸(`.taskbar-spacer`)이 **트레이와 같은 `flex: 1 1 0`**이라 버튼이 나타나도 가운데 묶음(시작 버튼+창 목록)이 안 밀린다 — 실측으로 양쪽 649px 동일 확인. 새 CSS는 `justify-content` 한 줄뿐이다: 버튼은 `.taskbar-panel`을 그대로 입는다(다만 토글이 아니라 `aria-pressed`·`-on`은 없다). 글자가 사라진 만큼 `title`+`aria-label`이 이름을 진다. 글리프는 새로 고침이 아니라 **되돌리기**(`mdi-light:undo-variant`) |
| 2026-08-07 | 스탯창 폭 **280 → 320px**, 성장 스탯 칸의 값에 `min-width: 3ch` | StatPanel, Desktop.css(`.stat-cell-value`), calendar(`statPanelReserve` 336), shell 주석, calendar.test·shortcuts.test | 사용자 지적("숫자 길어지면 글자 짤리는 거 아니냐") — **맞았다.** 칸에서 `flex: 1`인 것은 이름뿐이라 값이 한 자리에서 세 자리로 늘면 그만큼을 라벨에서 빼앗아 갔다(280px에서는 값이 0일 때 이미 '예의범절'이 잘렸고, 999가 되면 3글자 라벨까지 잘린다). **실측으로 폭을 정했다**: 최악의 칸이 135px 필요(아이콘 14 + 이름 38 + 등급 22 + 값 27 + 간격 18 + 패딩 16)인데 280px에서는 115px뿐 → 320px. `min-width: 3ch`는 폭을 늘리는 것과 별개로 **라벨 폭을 값과 무관하게 고정**한다(안 잡으면 스탯이 오를 때마다 같은 화면이 다르게 읽힌다). 폭만 늘렸고 **세로는 한 줄도 안 건드렸다**(544px 유지 — 스탯창 세로 스크롤바 금지). `shortcuts.test`가 프로그램 아이콘 개수를 행 번호로 박아 두고 있어 파생으로 바꿨다 |
| 2026-08-07 | 바탕화면 항목 정리 — '이벤트 도감' → **'사진첩'**(이름·아이콘 `fluent-color:image-24`), 프로그램 열에 **포토샵·VS 코드** 추가(stub) | desktopItems, ExplorerApp | 사용자 지시. **폴더 id `codex`는 그대로 뒀다** — 옮겨 둔 아이콘 위치(`desktopIconStore`)가 그 id로 저장돼 있고 `FolderId` 타입·`GameState.events`도 같은 id를 쓴다. 새 프로그램 둘은 **`kind: 'stub'`** — 정해진 승격 경로이고(구현되면 kind만 바꾼다) `desktopItems.test.ts`가 안내 문구 없는 빈 창을 막는다. 배치는 한 줄도 안 건드렸다: `DEFAULT_ICON_CELLS`가 `kind !== 'folder'`로 열을 파생하므로 배열에 넣기만 하면 왼쪽(프로그램) 열에 붙는다 |
| 2026-08-07 | 네이놈 포털 **상단 쇼핑 띠** 신설 — 뉴스 위에 [제목 + 가게 링크 ···· 쪽 번호] / [회색 구역 목록 | 진열 타일 5칸] 상자 하나 | sites(`STORE_SITES` 파생, `PROMO_SITES`가 그만큼 뺀다), NeverPortal(tsx `ShopStrip`+`PromoCard`, css), sites.test | 사용자 지시(레퍼런스: 실제 포털 쇼핑 섹션). 물건 파는 곳이 하단 소개 카드 일곱에 섞여 스크롤 끝까지 내려가야 보였다. **`Site`에 플래그를 안 더한 것**이 핵심 — "물건을 파는 곳"은 `render`(`'shop'|'tech'`)가, "활동을 여는 물건인가"는 `activitiesUnlockedBy`가 이미 아는 사실이라 둘 다 파생시켰다(진열 축을 새로 만들면 가게 화면과 포털이 갈린다). 왼쪽 회색 칸은 레퍼런스의 쇼핑몰 목록 자리인데 **실제로 타일을 거르고**, [장바구니] 자리에는 갈 데 없는 링크 대신 배송 규칙을 사실로 적었다. ⚠️ **여기서는 아무것도 못 산다** — 타일은 그 물건을 파는 사이트로 갈 뿐이다(브라우저가 상태를 바꾸는 자리를 늘리지 않는다). ⚠️ 실측이 잡은 것: `minmax(140px,1fr)`이 4칸으로 접혀 다섯 번째 타일이 아랫줄로 떨어졌다(112px로 고쳤다) |
| 2026-08-07 | 네이놈쇼핑·하이마루의 진열을 **구역(섹션) 판형**으로 — 평평한 카드 격자를 '제목 띠 + 그 아래 진열' 상자로 접었다 | ShopSite(tsx+css), TechSite(tsx+css) | 사용자 지시(레퍼런스: 포털 쇼핑 섹션). **분류 축을 새로 만들지 않은 것**이 핵심이다 — '스탯 상승 / 활동 해제'는 `activitiesUnlockedBy`가 이미 파생시키던 사실이고(하이마루 카드의 `.tc-spec` '종류' 칸이 이미 그걸 말하고 있었다), `ShopItem`에 카테고리 필드를 더했으면 같은 사실이 두 곳에 적혀 한쪽만 고치는 사고가 난다. 두 사이트가 **같은 축**으로 갈려 같은 규칙으로 읽힌다. 빈 구역은 그리지 않는다(제목만 있는 상자는 장식). 구역 상자에 배경을 안 깐 것은 흰 카드가 같은 색에 잠기기 때문 — 띠만 흰 판이고 본문은 페이지 바탕색이다. ⚠️ 실측이 잡은 것: 상자 패딩이 격자 판을 32px 좁혀 **375px에서 `minmax(320px,1fr)`이 가로 스크롤을 만들었다**(`min(320px, 100%)`로 고쳤다) |
| 2026-08-07 | 은행 페이지를 **탭 3개**(예금·대출·거래 내역)로 접었다. 잔액 요약(KPI)만 상시 | BankSite(tsx+css) | 사용자 지시. 네 구역이 세로로 쌓여 대출 한도를 보려면 예금 카드 둘을 지나야 했다. **KPI 줄만 탭 밖에 남긴 것**이 핵심 — 어느 탭에 있든 "지금 얼마인가"가 판단의 전제이고, 빚 칸이 상시로 붉게 알리므로 대출 경고를 탭 뒤에 숨겨도 위험이 사라지지 않는다. 안 보이는 탭은 언마운트가 아니라 **`hidden`**이다(치던 금액이 탭을 오갈 때 날아가지 않는다). 탭 줄은 로빙 탭인덱스 + 좌우 화살표 — Tab 키가 탭 3개를 다 훑고 나서야 본문에 닿으면 마찰이 늘어난다 |
| 2026-08-07 | **사이트의 확정 패널(`ActivityCommit`) 전면 폐기** — 항목을 누르면 실행 확인창(`ActivityConfirm`)이 바로 뜬다. 사이트 8종 전부 | ActivityConfirm(+css, 프롭 6개 추가), ActivityCommit(삭제), LibrarySite·CinemaSite·PublishSite·AlbamonSite·CampusSite·CertSite·FleaSite·TwitterSite(+css 3), activityPreview·BrowserApp·BankSite·sites 주석 | 사용자 지시("~하기 섹션 다 빼"). 사이트마다 화면 아래에 붙어 있던 큰 확정 카드가 목록을 밀어내고 있었다. **새 확인창을 만들지 않고 바로 가기 확인창을 일반화한 것**이 핵심 — 그 창이 이미 넷의 약속(증감·번아웃·조건·오후 생활비)을 `previewActivity`/`previewWarnings`로 지고 있어서, 새로 만들면 반드시 하나를 빠뜨린다. 사이트가 덧붙이는 것은 문구와 `notes`·`blocked`뿐이고 판정은 여전히 `canRun` 하나다. ⚠️ 잃을 뻔한 것 둘을 되살렸다: **바탕화면 등록**(구 패널의 우클릭 메뉴 → 확인창 안 버튼)과 **면접·출근**(고를 항목이 없어 '내 채용 현황' 카드에 `.flea-act` 버튼을 뒀다). 수강료·응시료는 활동 비용이 아니라 미리보기에 안 잡히므로 `notes`로 넘겨 같은 창에서 알린다 |
| 2026-08-06 | **하이마루(전자기기 사이트) 신설** — 쇼핑의 전자기기 3종을 옮기고 기기 4종 추가(잠금 해제형 1 + 고가 스탯형 3). 개인방송 활동 신설 | items(`store`·`buyableFor`·`storeNameOf`), TechSite(신규), sites, activities(`stream`), ShopSite, BrowserApp, activityPreview, SchedulerApp | 사용자 지시. 물건 파는 곳이 둘이 되면서 **진열 축(`store`)만 더하고 `SHOP_ITEMS`는 단일 출처로 남겼다** — 사이트별 배열을 따로 두면 같은 물건이 두 가게에 뜨는 상태가 가능해지고, 옮기는 비용도 한 줄이 아니게 된다. 물건 id를 그대로 둔 것은 기존 세이브의 인벤토리가 그 id를 들고 있기 때문. 잠금 사유 문구가 "쇼핑에서 구입"으로 굳어 있어 `storeNameOf` 파생으로 바꿨다 — 안 바꾸면 전자기기를 옮긴 순간 화면이 거짓을 말한다. ⚠️ QA가 잡은 것 둘: 방음 부스 주석의 총합이 34로 적혀 있었으나 **실제 46**(주석을 고치고 가격이 그것을 지탱하는 근거를 남겼다), 듀얼 모니터가 노트북과 **같은 아이콘**이었다(진열·인벤토리에 나란히 떠 구분 불가 — `delivery.test.ts`가 순회로 막게 했다) |
| 2026-08-06 | 모바일 폰 셸 **2차** — 아웃룩을 폰에서 목록↔본문 전환(1단)으로 접고, 브라우저 크롬을 주소창 한 줄로 축약(130px → 61px) | MailApp(tsx+css), BrowserApp.tsx(`browser-btn-forward`), MobileShell.css | 375px 전수 실측 결과 **사이트 13종은 이미 안 넘쳤다**(기존 `@container` 관행 덕). 실제로 깨진 것은 아웃룩 3단(61px 넘침, 본문이 화면 밖)과 세로를 먹는 브라우저 크롬 둘뿐이었다. 규칙을 `BrowserApp.css`가 아니라 셸 쪽에 둔 것은 데스크톱을 한 글자도 안 바꾸기 위해서다. ⚠️ 가장자리 앱의 패딩을 0으로 만들 때 **탐색기를 함께 넣었다가 415px로 삐져나갔다**(음수 마진이 상쇄할 것을 잃는다) — 실측으로 잡아 되돌렸다 |
| 2026-08-06 | **모바일 셸 뼈대 신설** — 폭 720px 이하면 가짜 스마트폰 UI(홈 그리드·전체화면 앱·상태바·하단바)로 갈아탄다. 수동 토글 2곳(시작 메뉴 / 폰 하단바) | shell(신규 systems+store+hooks), mobile/(신규 컴포넌트 5), appForWindow(신규 — kind 분기 단일 출처), App, WindowManager, data/shell·layers·icons, index.css(`--mo-*`), StartMenu, ToastHost.css, 앱 CSS 5(`--vh`) | 사용자 요구("모바일로 접속하면 휴대폰 UI"). 게임 로직·앱 내용물을 한 줄도 안 고치고 **껍데기만** 갈아 끼운 것은 `systems`/`data`가 화면을 모르기 때문이다. kind→컴포넌트 분기를 `appForWindow` 하나로 모은 것이 핵심 — 두 벌이면 새 앱을 추가할 때 반드시 한쪽을 빠뜨리고, 그때 **빌드도 테스트도 통과한 채** 빈 창이 뜬다(`Uncovered extends never` 가드가 컴파일 타임에 잡게 했다). 앱들의 `calc(100vh - N)`이 데스크톱 크롬 기준이라 `--vh`로 기준만 바꾼 것도 같은 판단이다: 앱마다 뺄셈을 두 벌씩 적으면 갈라진다 |
| 2026-08-06 | 트위터 사이트 신설 — 3열 타임라인(추천/팔로잉 탭·검색·트렌드 필터). 공사중 페이지가 하나도 남지 않았다 | tweets(신규 data+test), TwitterSite(신규), sites(`render: 'twitter'`+`activityId: 'sns'`), BrowserApp, sites.test | 레퍼런스는 실제 X. **평판 시스템은 만들지 않았다**(설계자 선택) — 트렌드는 `TRENDING_TERMS` 재사용, 팔로워는 `reputation` 파생이라 새 상태가 0개다. 구독 박스·팔로우 추천·갈 데 없는 네비를 뺀 것은 **장식 금지 규칙** 때문이고, 작성창 자리에 `ActivityCommit`을 그대로 놓은 것은 확정 UI를 새로 만들면 그 패널이 지는 약속 넷 중 하나를 반드시 빠뜨리기 때문. 사이트는 창 **안**이라 반응형을 뷰포트가 아니라 **컨테이너 쿼리**로 판정한다(미디어쿼리는 창을 줄여도 영영 안 접힌다 — 실측으로 잡았다) |
| 2026-08-06 | O넷(자격증 시험) 신설 — 종목 4종, **합격은 응시일이 아니라 발표일에 그날의 스탯으로** 확정. 자격증이 상위 공고 2개의 지원 자격 + 활동 2종을 연다. 포털 소개 카드에서 쇼핑을 한 칸 밀고 그 자리를 가져갔다 | certs·certification(신규 data+systems+test), CertSite(신규), careers(`Career.cert`), employment(`shortfalls` 3번째 인자), FleaSite, items, activities, sites, MailApp, gameStore, types, balance.verify | 급여 상위 공고에 "며칠 전에 미리 응시해 뒀는가"를 묻는 축을 만들기 위함 — 스탯은 시간을 들이면 반드시 오르지만 자격증은 계획성을 묻는다. 발표일 판정에 무작위를 안 넣은 것은 정규직과 같은 이유(탈락 사유를 말해 줘야 하는데 주사위를 섞으면 그 설명이 거짓이 된다). **새 알림 창구를 안 만들었다**: 통지는 아웃룩, 자격증은 택배 토스트를 그대로 탄다 |
| 2026-08-05 | **스탯 랭크 F·C·B·A·S·SS** 신설 + **예의범절 스탯** 추가(상한 100, 예절 교육·다도 모임 2종이 올린다) | rank(신규 systems+test), types, turn(`growthCap`), statMeta, activities, StatPanel, Desktop.css | 설계자 지시. 랭크를 **상한 대비 비율**로 잰 것은 상한이 다른 스탯(평판 100 / 지식 999)을 나란히 읽게 하기 위함 — 절대값으로 재면 "지식 A와 평판 A 중 무엇이 더 대단한가"에 답할 수 없다. 예의범절을 자원 줄이 아니라 그리드에 둔 것은 **실측 결과다**: 자원 줄에 두면 패널이 589px이 되어 720px 화면에서 스탯창에 세로 스크롤바가 생겼다(544px로 회수) |
| 2026-08-05 | 슬로우캠퍼스(온라인 강의) — 강의 8종 **단건 결제**, 같은 강의 3회 수강 시 수료증 아이템 발급 → 잠긴 외주 활동 2종 해금 | courses(신규 data+systems), CampusSite(신규), items(`buyable`·`BUYABLE_ITEMS`), activities(외주 2종), sites, types, gameStore, ShopSite | 미구현 사이트 둘 중 하나. 구독은 설계자 지시로 제외했다(지속 상태는 밤 정산이 필요해 은행·이사와 같은 무게가 된다). 수료증의 번아웃 키를 알바(`work`)와 가른 것은 `WORK_ACTIVITIES`에 섞이면 "알바는 넷"·"조건 없는 알바는 편의점뿐" 불변식이 깨지기 때문 |
| 2026-08-05 | 이사(방구석부동산) + 복권(쇼핑 판매대) 신설. **생활비를 `물가 구간 × 집 배율`로 재구성** | economy(`getLivingCost(state)`·`livingCostForDay`·`tierCostFor`), housing/lottery(신규 data+systems), RealtySite(신규), ShopSite, turn, autoAdvance, gameStore, StatPanel 등 생활비를 읽는 곳 전부 | 잘한 플레이가 시간을 벌어야 하는데 장치가 은행뿐이었다. 이사는 죽음의 원인(생활비)을 직접 깎고 복권은 분산을 준다. 생활비가 플레이어 상태에 의존하게 되면서 **읽는 곳이 하나라도 옛 경로에 남으면 그 화면만 거짓 숫자를 말하므로** 호출부를 전부 옮겼다 |
| 2026-08-05 | 밤 정산 순서 수정 — 급여가 우선한다. 게임오버는 밤이 다 정산된 뒤 한 번만 확정 | turn(`nightPayoutPending`·`settleGameOver`), employment(`advanceEmployment` 말미), gameStore, employment.test | 파산 판정이 생활비 차감과 급여 입금 **사이**에 있어, 급여일이 잔고 바닥나는 밤과 겹치면 월급 167만원을 손에 쥔 채 "파산"이 떴다 |
| 2026-08-05 | project-context 다이어트 493 → 250줄 (규칙 전량 보존, 실측·기각안·경위 서술 삭제) + "규칙만 적는다" 작성 규칙 명문화 | skills/project-context | 모든 에이전트가 코드를 만지기 전에 이 문서를 통째로 읽는데, 길이가 늘어 작업마다 수 분씩 지연됐다. 규칙과 그 규칙을 알아낸 보고서를 한 문서에 섞은 것이 원인 |
| 2026-08-05 | 디자인 스킬·CDP 실측을 신규 화면으로 한정, 검증 분량 규칙 신설, 컨텍스트 문서 250줄 상한 | CLAUDE.md, game-pipeline, project-context | 작업당 20~30만 토큰·20~30분이 들었고, 원인은 모든 작업에 685줄 스킬 로드 + 실클릭 검증 + 40개대 테스트를 요구한 것이었다 |
| 2026-08-05 | 자동 진행 — 멈출 이유가 생길 때까지 슬롯을 계속 넘기고 요약을 보여 준다 | data/systems/autoAdvance(신규), AutoLogApp(신규), gameStore, CalendarPanel, schedule | 판을 1년까지 늘리기 전에 "길어진 판이 견딜 만한가"를 먼저 재기 위함. 매 슬롯 클릭이 강제되면 날짜만 늘고 깊이는 안 는다 |
| 2026-08-05 | 활동 바로가기(확정 버튼 우클릭 → 바탕화면 등록 → 더블클릭 실행) + 공용 ContextMenu | shortcuts/contextMenu(신규 systems), shortcutStore(신규), ContextMenu·ActivityConfirm(신규), desktopItems, Desktop, ActivityCommit, activityPreview | 설계자 지시. 경고 문구를 `previewWarnings`로 모은 것은 확정 화면이 셋이 되어 한 곳만 빠뜨리는 사고를 막기 위함 |
| 2026-08-05 | 직업 엔딩 5종 — 취직이 아니라 **파산했을 때** 뜬다. `bigtech`의 스탯 조건 삭제, `GameState.peakCareerId` 신설, 세이브 version 2 | endings, careers, ending, employment, gameStore, types, EndingModal | 설계자 지시("직업엔딩은 취직한 순간이 아닌 돈 없어서 죽은 후 뜨게 해"). 취직은 결말이 아니라 도중의 사건이고, '대기업 합격'이라는 이름이 스탯 문턱과 청람그룹 입사 **두 가지**를 뜻하고 있었다 |
| 2026-08-05 | 알바 4종 + 알바몬 사이트 신설, `Activity.burnoutKey`로 번아웃 키 공유 | activities, jobs(신규), AlbamonSite(신규), sites, burnout, turn, SchedulerApp | 설계 문서의 "고소득 알바 전환 압박"이 알바가 하나뿐이라 구현되지 않았음. 알바를 늘리면 번갈아 일해 번아웃을 우회할 수 있어 키를 묶음 |
| 2026-08-04 | `Window`에 `bareTitle`·`dark` 옵션, 열릴 때 위치 클램프. HUD 테두리 장식 제거 | Window, WindowManager, HudPanel | 레퍼런스 충실도 + 창이 작업표시줄 아래로 빠지던 문제 |
| 2026-08-04 | 스케줄러(예약 자동 실행) + 포털 검색 결과 + 브라우저 개발자 모드 | schedule(신규), search(신규), SchedulerApp, NeverPortal, BrowserApp | 설계자 지시 |
| 2026-08-04 | 시작 메뉴 + 시스템 도구 3종(저장·작업관리자·명령프롬프트), 스탯 `gaming` 추가 | startMenu, StartMenu, SystemApps, types/statMeta | 설계자 지시 |
| 2026-08-04 | 메신저(카톡·너아무튼온)·아웃룩·토스트 알림 신설 | messages(데이터/시스템), toastStore, ChatApp, MailApp, ToastHost | 설계자 지시 |
| 2026-08-03 | NEVER→네이놈 개명, 로고 서체(SB 어그로), 검색창·퀵메뉴·뉴스 리스타일, 배너존 + 광고 보상(하루 1회 100원) | sites, banners(신규), news, turn.ts, gameStore, NeverPortal | 설계자 지시 |
| 2026-08-03 | 주소창 입력 가능화(`resolveUrl`), NEVER 포털 모던 리스타일 | sites.ts, BrowserApp, NeverPortal.css | 설계자 지시 |
| 2026-08-03 | 스탯 개명(행동력/체력), 평판·도덕 상한 100 + 자원 줄 이동, 트레이 버튼 토글화, 낮/해질녘 배경, 브라우저 흰 크롬·기본 즐겨찾기 제거 | turn.ts(growthCap), StatPanel, desktopPanelStore, Desktop, browserStore | 설계자 지시 |
| 2026-08-03 | HUD 아이콘 세트 ph → mdi 교체(`@iconify-json/ph` 제거), 구역 라벨 전부 삭제, 브라우저 탭 줄 추가 | statMeta, icons, StatPanel, BrowserApp, build-icon-subset | 설계자 지시 |
| 2026-08-03 | 테두리 장식 추가 (HUD·게임 팝업·엔딩 모달, 브라우저 제외) | PanelOrnament(신규), Window/WindowManager, HudPanel, EndingModal | 설계자 지시. 외부 이미지 대신 인라인 SVG |
| 2026-08-04 | 폰트 5벌 전부 CDN → 번들 셀프호스팅 | index.css, index.html, src/assets/fonts/(신규), package.json | CDN 지연·오프라인 불가로 첫 화면이 흔들림. 아이콘의 오프라인 규칙과 불일치 |
| 2026-08-04 | UI 경량/풀 경로 분리 + 에이전트 모델 상속 규칙 | CLAUDE.md, game-pipeline | 소규모 스타일 수정에도 풀 파이프라인이 돌아 작업이 느려짐 |
| 2026-08-03 | 브라우저 즐겨찾기 줄·별표·더보기 메뉴 추가, 스탯창 스크롤 제거·"생계" 제목 삭제 | BrowserApp, browserStore(신규), HudPanel, StatPanel | 설계자 지시 |
| 2026-08-03 | HUD 밝은 모던 리스타일 완료 + 폰트 3벌(Pretendard/Cafe24 Ohsquare/SF 함박눈) 도입 | src/index.css, HudPanel/Desktop/EndingModal CSS, index.html | 다크 판타지 방향 기각분 마무리, 설계자 폰트 지시 반영 |
| 2026-08-04 | 쇼핑·택배·인벤토리/이벤트 도감 추가, 카톡 일반/오픈 탭 분리, 일정 창 전체 높이화 | src/, project-context | 설계자 요청 배치 실행 |
| 2026-08-04 | 활동 15종·분류·아이템 잠금 규칙 반영 | project-context 스킬 | 활동 8종 추가로 성장 스탯 10종 전부 육성 가능해지고, `Activity.category`/`requiresItem`이라는 새 컨벤션이 생김 |
| 2026-08-03 | 디자인 규칙 추가: UI 작업 전 ui-ux-pro-max 필수 로드 | CLAUDE.md, game-pipeline | 디자인 스킬을 설치했는데 참조하지 않고 감으로 스타일링한 문제 재발 방지 |
| 2026-08-03 | 파일 맵·컨벤션·구현 중 결정 사항 반영 | skills/project-context | 다음 세션의 코드베이스 재탐색 방지 |
| 2026-08-03 | 플레이 가능 코어 구현 완료 (Task 1~12) | src/ 전체 | 계획 2026-08-03-playable-core 실행 |
| 2026-08-03 | project-context에 창 런타임 상태·캡션 버튼 회귀·윈도우 11 시각 규칙 추가 | project-context 스킬 | 최대화가 런타임 상태로 바뀌고 캡션 버튼 3종이 생겨 컨벤션이 변경됨 |
| 2026-08-03 | 초기 구성 (dev/qa 에이전트, pipeline/context 스킬) | 전체 | 토큰 최적화 하네스 신규 구축 |
