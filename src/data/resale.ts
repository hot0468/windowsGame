/**
 * 중고마켓(두손마켓) 시세.
 *
 * ⚠️ **물건 값을 여기 다시 적지 않는다** — 정가의 단일 출처는 `data/items.ts`의
 * `ShopItem.price`이고 여기 있는 것은 **비율 하나**다. 물건마다 중고가를 손으로 적으면
 * 값을 올릴 때 두 곳을 고쳐야 하고, 한쪽만 고치면 사자마자 되팔아 이득이 나는 물건이 생긴다.
 */

/** 정가 대비 매입가. 설계자 지시: **반값**. */
export const RESALE_RATE = 0.5

/**
 * 포스트카드의 "정가". **포스트카드는 파는 물건이 아니라 표에 딸려 오는 굿즈라 정가가 없다** —
 * 그래서 굿즈 한 장 값을 여기서 한 번만 정한다(관람료 15,000원과는 다른 축이다:
 * 영화값은 활동이 이미 받아 갔고 카드는 덤이다).
 *
 * ⚠️ 이 값을 올릴 때는 **영화를 보는 것이 돈벌이가 되는 선**을 넘지 않는지 본다 —
 * 관람료보다 매입가가 커지면 극장이 아니라 현금 인출기가 된다.
 */
export const POSTCARD_LIST_PRICE = 8_000
