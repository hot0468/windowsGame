/**
 * 웹툰 연재 수치.
 *
 * ⚠️ **실존 플랫폼 금지**(전부 지어낸 상호).
 * ⚠️ 원고료는 **물가 배율을 타지 않는다** — 정규직 급여·트위터 정산·외주 보수와 같은
 * 장치다. 연재가 물가를 따라 오르면 후반에도 살아남는 수입원이 되어 판이 안 끝난다.
 */

/** 연재처 이름. 제의 메일과 클립스튜디오 화면이 같은 문자열을 쓴다. */
export const STUDIO_NAME = '주간 페이지'

/** 연재작 가제. 제의 때 정해지고 바뀌지 않는다(플레이어에게 입력을 요구하지 않는다). */
export const SERIES_TITLE = '이름 없는 골목'

/**
 * 제의가 오는 조건 — **둘 중 하나**면 된다(설계자 지시).
 *
 * ⚠️ **좋아요는 팔로워가 아니다**(`TwitterState.likes`). 팔로워로 판정하면 평판만 올려도
 * 제의가 와서 "그림을 그려서 알려졌다"가 거짓이 된다.
 * ⚠️ 공모전 쪽은 **입상 1회**면 된다 — 낙선은 세지 않는다.
 */
export const LIKES_FOR_OFFER = 4_000
export const CONTEST_WINS_FOR_OFFER = 1

/** 한 주에 넘겨야 하는 원고 수. 한 장이 1턴이므로 **일주일 슬롯 14개 중 3개**다. */
export const WEEKLY_PAGES = 3

/** 마감 주기(일). 설계자 지시: 일주일. */
export const DEADLINE_DAYS = 7

/**
 * 한 주를 채웠을 때 받는 원고료.
 *
 * ⚠️ **`WEEKLY_PAGES`로 나눈 값이 턴당 수익이다**: `240,000 ÷ 3 = 80,000원/턴`.
 * 이것은 알바보다 세지만 **끝나지 않는 마감이 대가다** — 매주 3턴을 여기에 묶어야 하고
 * 놓치면 평판이 깎이며, 두 번 놓치면 연재가 끝난다. 물가 배율을 안 타므로 후반에는
 * 이 수입만으로 못 산다(`webtoon.test.ts`가 마지막 물가 구간과 견줘 지킨다).
 */
export const EPISODE_PAY = 240_000

/** 마감을 놓치면 깎이는 평판. 그몽과 같은 규칙 — **위약금이 아니라 평판이다**. */
export const MISS_REPUTATION_PENALTY = 4

/**
 * 이만큼 놓치면 연재가 끝난다.
 *
 * ⚠️ **정규직 해고(6회)보다 훨씬 빠르다.** 회사는 사람을 뽑는 데 절차가 들지만 연재는
 * 다음 주에 다른 작가를 올리면 되기 때문이고, 게임 규칙으로는 "주 3장이 가볍지 않다"를
 * 몸으로 알리는 장치다. 끝나면 **다시 제의가 오지 않는다** — 그것이 수락의 무게다.
 */
export const MISSES_TO_END = 2

/* ── 월요일 연재 리뷰 (2026-08-08) ──────────────────────────────────────────
 *
 * 연재 중이면 **월요일마다 담당 편집자가 카톡으로** 지난 회차의 조회수와 세간의 평가를
 * 알려 준다(설계자 지시).
 *
 * ⚠️ **조회수는 돈을 만들지 않는다.** 원고료의 단일 출처는 `EPISODE_PAY` 하나이고
 * 여기서 나오는 숫자는 **읽히고 있다는 사실**만 전한다 — 조회수에 수익을 붙이면 한 회차가
 * 두 번 벌게 되고(너튜브 구독자에 정산을 안 붙인 것과 같은 판단) 상한도 두 곳으로 갈린다.
 *
 * ⚠️ `Math.random` 금지 — 조회수도 평가도 **상태의 순수 함수**다(`systems/webtoon.ts`).
 */

/** 담당 편집자. 카톡 방 이름이자 말하는 사람이다. */
export const EDITOR_NAME = '차 편집자'

/** 아무도 안 봐도 찍히는 바닥 조회수. 연재처가 걸어 주는 몫이다. */
export const VIEW_BASE = 12_000

/**
 * 점수 1점이 데려오는 독자 수. 점수는 그림 실력과 평판에서 나온다.
 *
 * ⚠️ 만점 근처(예술 999 + 창의력 999 + 평판 100×3)면 약 92만이고 고정 독자가 얹힌다 —
 * **인기 웹툰의 자릿수**가 나오도록 맞춘 값이다. 900이었을 때는 중간 실력에서 이미
 * 39만이 찍혀 "화제작"이 기본값처럼 읽혔다(실측).
 */
export const VIEW_PER_SCORE = 400

/**
 * 회차가 쌓이면 붙는 고정 독자 비율(회차당).
 * ⚠️ 곱이 아니라 **덧셈 비율**이다 — 곱하면 회차 수에 지수로 커져 20회차쯤에 억 단위가 된다.
 */
export const VIEW_PER_EPISODE = 0.08

/**
 * 세간의 평가. **점수 구간마다 배타적으로 하나**를 쓴다(`STREAM_REVIEWS`와 같은 규칙) —
 * 누적으로 섞으면 "화제작"과 "아무도 안 본다"가 같은 주에 함께 뜬다.
 *
 * ⚠️ 편집자의 말투는 **일관되게 사무적이다**. 응원도 비난도 하지 않고 반응을 옮긴다 —
 * 그것이 담당 편집자라는 자리의 성격이고, 플레이어가 숫자를 스스로 해석하게 둔다.
 */
export const EPISODE_REVIEWS: { tier: 0 | 1 | 2; text: string }[] = [
  { tier: 0, text: '댓글은 조용합니다. 초반 회차는 원래 이렇습니다.' },
  { tier: 0, text: '체류 시간이 짧습니다. 첫 컷에서 이탈이 많다는 뜻입니다.' },
  { tier: 0, text: '평점은 아직 표본이 적어 의미가 없습니다. 다음 주에 다시 보겠습니다.' },
  { tier: 1, text: '고정 독자가 생겼습니다. 지난주 본 사람이 이번 주에도 왔습니다.' },
  { tier: 1, text: '댓글에 "골목 배경이 좋다"는 말이 여럿 있었습니다.' },
  { tier: 1, text: '추천 탭에 한 번 걸렸습니다. 유입이 평소의 두 배였습니다.' },
  { tier: 2, text: '커뮤니티에 컷이 돌고 있습니다. 출처 없이 돌아 아쉽지만 좋은 신호입니다.' },
  { tier: 2, text: '단행본 문의가 들어왔습니다. 아직 답은 보류해 두었습니다.' },
  { tier: 2, text: '이번 주 신작 순위 상위권입니다. 다음 회차 일정을 지켜 주십시오.' },
]
