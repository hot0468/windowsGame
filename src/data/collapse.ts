/**
 * 무너짐 — 체력·멘탈이 바닥났을 때의 수치(2026-08-22 설계자 지시).
 * 규칙은 `systems/collapse.ts`가 갖는다. 여기는 숫자만 있다.
 */

/**
 * 이 멘탈 아래에서만 랜섬웨어 위험이 생긴다.
 *
 * ⚠️ **낮게 잡는다(20).** 아무 때나 걸리면 "멘탈을 지켜라"가 아니라 그냥 세금이 되고,
 * 감염 자체가 하루 `MALWARE_DAILY_LOSS`씩 새는 사건이라 벌이 두 겹이 된다.
 */
export const RANSOM_MENTAL_FLOOR = 20

/**
 * 멘탈이 **0일 때**의 하루 감염 확률.
 *
 * ⚠️ 0.35는 "거의 확실"과 "운 나쁘면"의 사이다 — 1에 가까우면 멘탈 0이 곧 감염이라
 * 확률로 둘 이유가 없고, 0.1 아래면 규칙이 있는지도 모른다. 문턱(20)까지 선형으로 준다.
 */
export const RANSOM_RISK = 0.35
