import type { EconomyTier } from '../types/game'

/**
 * 10일 주기 계단식 물가 인상.
 * 표 구간(1~51일차) 안에서는 알바비가 생활비보다 느리게 올라 **초반의 압박**을 만든다 —
 * 저임금 알바만으로는 점점 빠듯해져서 스탯에 투자할 이유가 된다.
 * day 내림차순이 아니라 오름차순으로 두고, 조회 시 역순 탐색한다.
 *
 * ⚠️ **표를 넘어선 뒤에는 둘이 같은 속도로 오른다**(아래 두 상수) — 표 구간의 격차는
 * 초반 난이도이지 영구적인 사형 선고가 아니다.
 */
export const ECONOMY_TIERS: EconomyTier[] = [
  { day: 1, living: 30000, wageMultiplier: 1.0 },
  { day: 11, living: 38000, wageMultiplier: 1.15 },
  { day: 21, living: 48000, wageMultiplier: 1.28 },
  { day: 31, living: 60000, wageMultiplier: 1.39 },
  { day: 41, living: 75000, wageMultiplier: 1.48 },
  { day: 51, living: 95000, wageMultiplier: 1.55 },
]

/** 표 이후 구간의 주기(일). 표와 같은 10일 간격을 이어간다. */
export const TIER_INTERVAL = 10

/**
 * 표 이후 구간마다 곱해지는 생활비 상승률.
 *
 * ⚠️ **`WAGE_GROWTH_RATE`와 반드시 같은 값이다. 둘을 갈라놓지 말 것**(2026-08-14).
 * 예전에는 1.3 대 1.04였고 그 격차가 10일마다 복리로 벌어져 **301일차 생활비가 하루
 * 6,700만원**이었다 — 최고 월급(460만원)의 열 배를 훌쩍 넘는다. 그건 경제가 아니라
 * **종말 시계**였고, 실제로 "물가 상승으로 후반 생존이 불가능해져 자연 종결"이
 * 이 게임의 종료 장치였다.
 *
 * 이 게임은 육성물이 됐다(설계자 지시). 판을 끝내는 것은 파산이 아니라 **생활 등급**이고,
 * 파산은 죽음이 아니라 며칠을 뺏는 사건이다(`Recovery`). 끝이 없는 판에서 곡선이
 * 발산하면 후반의 모든 선택이 무의미해지므로, 두 상승률을 맞춰 **실질 부담을 수렴**시킨다
 * (생활비 ÷ 알바 배율이 61,290원/일에 고정된다).
 *
 * 그래도 숫자는 계속 커진다 — 물가가 멈추면 경제가 죽은 것처럼 보이기 때문이다.
 * 오르는 것은 **자릿수**고, 변하지 않는 것은 **부담**이다.
 */
export const LIVING_GROWTH_RATE = 1.08

/**
 * 표 이후 구간마다 곱해지는 알바비 상승률.
 * ⚠️ **`LIVING_GROWTH_RATE`와 같은 값을 유지한다** — 사유는 그쪽 주석에 있다.
 */
export const WAGE_GROWTH_RATE = 1.08

/**
 * 주말(토·일) 알바 시급 할증.
 *
 * ⚠️ **요일이 처음으로 돈에 붙는 자리다**(2026-08-09). 여태 요일은 주간 예약과 정규직
 * 주말 호출에만 쓰여, 하루를 고를 때 요일을 볼 이유가 없었다.
 *
 * ⚠️ **주말에만 붙는 할증이지 상승률이 아니다.** 주 2일이라 평균 +8.6%이고, 이 값은
 * 구간이 지나도 커지지 않는다 — 물가와 알바비가 같은 속도로 오르는 지금(`LIVING_GROWTH_RATE`)
 * 실질 부담은 수렴하므로, 이 할증은 **"주말에 일하면 이득"이라는 요일 선택**을 만드는
 * 장치일 뿐 곡선을 흔들지 않는다. 그래도 **감이 아니라 시뮬레이션이 지킨다**
 * (`balance.verify.test.ts`). 값을 올리려면 그 테스트를 먼저 보라.
 * ⚠️ **알바에만 붙는다**(`scalesWithWage`). 월급·그몽 보수·밴드 보수는 물가 배율 자체를
 * 안 타므로 여기도 안 탄다.
 */
export const WEEKEND_WAGE_BONUS = 1.3
