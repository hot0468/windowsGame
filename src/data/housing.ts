/**
 * 이사 — 집을 옮겨 **생활비를 영구히 낮춘다.**
 *
 * ## 이 장치가 있는 이유
 * 이 게임에서 플레이어를 죽이는 것은 언제나 **생활비**다(10일마다 오르고 결국 아무도
 * 못 버틴다). 은행은 그 죽음을 며칠 **미루는** 장치였다면, 이사는 죽음의 원인 자체를
 * 건드리는 유일한 장치다 — 생활비 배율이 내려가면 그 뒤의 모든 물가 인상이 함께 줄어든다.
 *
 * ## ⚠️ 생활비는 이제 순수한 날짜 함수가 아니다
 * 예전에는 `getLivingCost(day)`가 전부였다. 지금은 **물가 구간(날짜) × 집 배율(플레이어
 * 상태)**이다. 물가 곡선은 그대로 위에 얹히므로 **싼 방에 살아도 인플레는 그대로 느낀다** —
 * 이사는 기울기를 바꾸는 것이 아니라 **상수를 줄이는 것**이다. 그래서 이사해도 판은
 * 반드시 끝난다(`balance.verify.test.ts`가 시뮬레이션으로 지킨다).
 *
 * ## 보증금은 돌려받는다
 * 한국의 보증금은 실제로 돌려받는 돈이고, 이 게임에서도 그렇게 모델링했다. 그러면
 * 보증금은 **버린 돈이 아니라 묶인 자본**이 되어 은행 예금과 같은 성격을 띤다 —
 * "이 돈을 방에 묶을 것인가, 정기예금에 넣을 것인가"라는 판단이 생긴다. 대신
 * **중개수수료(`fee`)는 돌아오지 않는다** — 되돌릴 수 없는 비용이 하나도 없으면
 * 이사가 공짜가 되어 매일 방을 옮기는 것이 최적해가 된다.
 *
 * ## 턴을 쓰지 않는다
 * 은행 거래·쇼핑 주문과 같은 규칙("탐색은 무료"). 이사의 비용은 슬롯이 아니라
 * **목돈이 묶이는 것**과 **더 싼 방일수록 다른 스탯을 갉는 것**이다.
 */

import type { IconName } from '../types/game'

/**
 * 살 수 있는 방 하나.
 *
 * ⚠️ **`rate`가 이 데이터의 본체다.** 생활비에 곱해지는 배율이고, `1`이 시작 지점이다.
 */
export interface Housing {
  id: string
  /** 화면에 뜨는 매물 이름. */
  name: string
  /** 동네. 매물이 어디에 있는지가 없으면 목록이 그냥 숫자표가 된다. */
  area: string
  icon: IconName
  /**
   * 생활비 배율. **1이 기본(원룸 전세)이고 내려갈수록 싸다.**
   * ⚠️ 물가 구간은 이 값 위에 그대로 곱해진다 — 싼 방도 인플레를 그대로 맞는다.
   */
  rate: number
  /**
   * 보증금. **이사 나갈 때 그대로 돌려받는다**(묶인 자본이지 버린 돈이 아니다).
   *
   * ⚠️ **싼 방일수록 크다**(아래 `HOUSINGS` 주석의 "곡선의 방향" 참조) — 돌려주는 돈으로
   * 곡선을 반대로 잡으면 첫날 고시원으로 내려가는 것이 공짜 이득이 되어 이사가
   * 판단이 아니라 절차가 된다.
   */
  deposit: number
  /**
   * 중개수수료. **돌아오지 않는다.** 이사가 진짜 결정이 되게 하는 유일한 매몰비용이다.
   */
  fee: number
  /** 매물 설명. 왜 싼지가 여기에 적혀 있어야 "싼 방에는 이유가 있다"가 읽힌다. */
  desc: string
  /**
   * 이 방에 살면서 치르는 대가(스탯).
   *
   * ⚠️ **싼 방이 순수한 이득이면 이사는 판단이 아니라 절차가 된다.** 지하방·고시원은
   * 생활비를 크게 깎는 대신 **취침 시 회복되는 멘탈을 갉는다** — 그래서 "얼마나 싼
   * 방까지 견딜 것인가"가 실제 선택이 된다. 규칙은 `systems/housing.ts`가 갖는다.
   */
  mentalPerNight: number
}

/**
 * 매물 목록. **배열 순서 = 싼 곳 → 비싼 곳**이고 첫 항목이 시작 집이다.
 *
 * ⚠️ **`HOUSINGS[0]`이 "이사한 적 없음"의 기본값이다**(`rate: 1`). 구버전 세이브에는
 * `housing` 필드가 아예 없는데, 그때 이 항목으로 읽으면 마이그레이션이 필요 없다
 * (`bank`·`plans`와 같은 규칙).
 *
 * ## ⚠️ 곡선의 방향이 뒤집혀 있다 — 싼 방일수록 **보증금이 비싸다**
 * 처음에는 현실을 따라 "싼 방 = 싼 보증금"으로 잡았다가 갈아엎었다. 보증금을 돌려주는
 * 이상 그 배치는 **첫날 고시원으로 내려가면 현금이 늘어난다**는 뜻이 되고, 그러면
 * 이사는 판단이 아니라 **모두가 1일차에 하는 절차**가 된다(설계자 요구: "명백한
 * 자동 구매가 아니어야 한다"를 정면으로 깬다).
 *
 * 그래서 게임의 논리를 택했다: **생활비를 깎아 주는 계약일수록 목돈을 크게 요구한다.**
 * 시작 원룸은 보증금이 가장 적고(50만) 월세로 그 값을 매일 치르는 집이며, 고시원은
 * 보증금 210만을 한 번에 묶는 대신 하루 지출이 절반 이하가 되는 집이다. 이야기로는
 * "전세에 가까울수록 목돈이 크고 매일 나가는 돈이 적다"이고, 게임으로는
 * **선불로 며칠을 사는 거래**다 — 은행 정기예금과 정확히 같은 성격이라 둘이 자리를 다툰다.
 *
 * ## ⚠️ 계약금은 이 게임이 **실제로 만들어 내는 현금** 안에 있어야 한다
 * 알바만 하는 최적 플레이의 소지금은 11일차 약 75만 → 31일차 약 166만 → 51일차 약 240만으로
 * 오르고 **71일차 약 265만이 정점**이다(그 뒤로는 물가가 이겨 내려간다). 계약금을 그 위로
 * 잡으면 그 매물은 **존재하지만 아무도 갈 수 없는 방**이 되고, 그건 데이터에만 있는 선택지다.
 * 그래서 가장 비싼 계약(고시원)도 정점의 절반 남짓에 두어, **중반에 목돈을 모으면 닿되
 * 그러는 동안 다른 데 쓸 돈은 사라지도록** 잡았다.
 *
 * ## 회수 기간 (계약 시점 생활비 기준, 물가가 오르면 더 빨라진다)
 *  - 빌라(0.86)   : 계약금  52만 · 20일차 생활비 기준 절감 6,700/일  → **약 78일**
 *  - 반지하(0.72) : 계약금  95만 · 30일차 기준 절감 16,800/일        → **약 57일**
 *  - 옥탑(0.62)   : 계약금 138만 · 40일차 기준 절감 28,500/일        → **약 48일**
 *  - 고시원(0.48) : 계약금 182만 · 50일차 기준 절감 49,400/일        → **약 37일**
 *
 * ⚠️ **회수 기간을 일부러 어긋나게 뒀다**(싼 방일수록 빨리 회수된다). 그렇지 않으면
 * "어느 방이든 똑같다"가 되어 다섯 개일 이유가 없다. 대신 싼 방일수록 **목돈이 크고
 * 멘탈 대가가 무겁다** — 그 셋이 맞물려야 "지금 내 형편에 맞는 방"이 사람마다 달라진다.
 * 물가가 오를수록 절감액이 커지므로 **늦게 갈수록 회수가 빠르지만 남은 날이 적다.**
 */
export const HOUSINGS: Housing[] = [
  {
    id: 'oneroom',
    name: '지금 사는 원룸',
    area: '역세권 도보 5분',
    icon: 'fluent-color:home-24',
    rate: 1,
    // 시작 집의 보증금은 **이미 낸 것으로 친다** — 그래야 이사 갈 때 돌려받을 것이 생긴다.
    // 목록에서 가장 **적다**: 목돈이 안 묶인 대신 매일 나가는 돈이 가장 많은 집이다.
    deposit: 500_000,
    fee: 0,
    desc: '보증금이 거의 없는 대신 다달이 값을 한다. 이 방이 비싼 이유는 방 때문이 아니라 계약 때문이다.',
    mentalPerNight: 0,
  },
  {
    id: 'villa',
    name: '언덕 위 빌라',
    area: '버스 두 정거장 + 도보 10분',
    icon: 'fluent-color:building-home-24',
    rate: 0.86,
    deposit: 900_000,
    fee: 120_000,
    desc: '언덕이 가팔라 시세가 눌렸다. 방은 멀쩡하고, 다리가 조금 고생할 뿐이다.',
    mentalPerNight: 0,
  },
  {
    id: 'semi',
    name: '반지하 투룸',
    area: '주택가 골목',
    icon: 'fluent-color:building-multiple-24',
    rate: 0.72,
    deposit: 1_300_000,
    fee: 150_000,
    desc: '넓다. 대신 창이 눈높이보다 낮고, 장마철 이야기는 아무도 먼저 꺼내지 않는다.',
    // ⚠️ 여기서부터 대가가 붙는다. 취침 회복 멘탈이 5인데 1을 떼면 회복의 20%다.
    mentalPerNight: 1,
  },
  {
    id: 'rooftop',
    name: '옥탑방',
    area: '구시가지 5층 위',
    icon: 'fluent-color:home-24',
    rate: 0.62,
    deposit: 1_700_000,
    fee: 180_000,
    desc: '전망 하나는 이 목록에서 제일 낫다. 여름과 겨울이 각각 자기 방식으로 그 값을 받아 간다.',
    mentalPerNight: 2,
  },
  {
    id: 'gosiwon',
    name: '고시원 단칸',
    area: '학원가 뒷골목',
    icon: 'fluent-color:building-24',
    rate: 0.48,
    // 목록에서 가장 **크다**. 생활비를 절반 이하로 만드는 계약의 값이다.
    deposit: 2_100_000,
    fee: 220_000,
    desc: '누우면 벽이 닿는다. 목돈을 한 번에 묶는 대신 하루하루 나가는 돈이 가장 적다.',
    // 절반 이하의 생활비를 사는 값. 취침 회복 멘탈 5 중 3을 가져간다.
    mentalPerNight: 3,
  },
]

/** 이사한 적 없는 사람이 사는 집. `state.housing`이 없을 때 이 값으로 읽는다. */
export const DEFAULT_HOUSING_ID = HOUSINGS[0].id

export function findHousing(id: string): Housing | undefined {
  return HOUSINGS.find((h) => h.id === id)
}

/** 부동산 사이트 이름. 화면 두 곳(사이트 제호·계약 내역)이 같은 문자열을 본다. */
export const REALTY_NAME = '방구석부동산'
