/**
 * 자동 진행(auto-advance) 수치.
 *
 * 게임 수치는 전부 `src/data/`에 산다는 규칙에 따라 여기 모은다.
 * 규칙(무엇이 멈춰 세우는가)은 `systems/autoAdvance.ts`, 화면은 날짜칸과
 * `components/apps/AutoLogApp.tsx`가 갖는다.
 */

/**
 * 한 번의 자동 진행이 넘길 수 있는 최대 슬롯 수(= 30일).
 *
 * ⚠️ **정지 조건이 하나도 발화하지 않아도 반드시 끝나야 한다.** 정지 조건은 앞으로
 * 늘어날 것이고(랜덤 이벤트), 그중 하나가 버그로 영영 false를 돌려주는 순간
 * 상한이 없으면 브라우저가 멈춘다. 그래서 상한은 "적절한 값"이 아니라 **안전장치**다.
 *
 * 30일로 잡은 근거: 물가 급등 사건의 평균 간격(`SHOCK_GAP` 30일)과 같다. 한 번의 진행이
 * 물가 구간을 셋이나 지나갔다면 그건 이미 "며칠 밀기"가 아니라 "한 달을 눈감고 살기"라
 * 어차피 사람이 한 번 확인해야 한다. 예약 연쇄 상한(`schedule.ts`의 `MAX_CHAIN` 40)과
 * 값이 다른 것은 의도다 — 저쪽은 한 번의 행동이 부르는 연쇄, 이쪽은 사람이 켠 진행이다.
 */
export const AUTO_MAX_SLOTS = 60

/**
 * 슬롯 하나를 넘기고 다음 슬롯까지 쉬는 시간(ms).
 *
 * 0으로 두면(동기 루프) 60슬롯이 한 프레임에 지나가 **날짜가 순간이동하고 멈출 수도 없다**
 * — ux `Loading States`("Don't: leave UI frozen with no feedback")와 정확히 같은 실패다.
 * 120ms면 날짜칸의 날짜가 넘어가는 것이 눈에 보이면서(ux `Progress Indicators`)
 * 상한까지 가도 7초가 조금 넘는다. 이 간격이 곧 **멈추기 버튼이 끼어들 수 있는 틈**이다.
 */
export const AUTO_STEP_MS = 120

/**
 * 소지금 위험선 = 하루 생활비 × 이 값.
 *
 * ## 왜 3일치인가
 * 파산은 **밤 정산**에서 일어난다(취침 시 생활비 차감, `systems/turn.ts`). 그래서 마지막으로
 * 손쓸 수 있는 자리는 항상 "그 전 오후 슬롯"이고, 위험선을 1일치로 잡으면 멈춘 순간
 * 이미 되돌릴 수 없다 — 알려 준 의미가 없다.
 *
 * 3일치 = **6슬롯**이다. 벌이가 되는 행동(알바)은 행동력을 25씩 먹어 하루에 두 번은
 * 못 하므로, 6슬롯은 곧 "취침 회복을 끼고 세 번은 더 벌어 볼 수 있다"는 뜻이다.
 * 2일치로 내리면 기회가 두 번뿐인데, 후반에는 알바 한 번의 실질 가치가 생활비 하루치
 * 아래로 떨어져 있어(알바비 상승률 < 생활비 상승률, `data/economy.ts`) 두 번으로는
 * 절대 못 돌아온다. 5일치로 올리면 평범한 플레이 내내 위험선 아래라 자동 진행이
 * 첫 슬롯마다 멈춰 기능 자체가 성립하지 않는다.
 */
export const MONEY_DANGER_DAYS = 3
