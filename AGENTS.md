# AGENTS.md — 이 저장소에서 일하는 에이전트에게

Claude Code 밖에서 이 저장소를 여는 에이전트(Cowork·클라우드 세션 등)를 위한 진입점이다.
`.claude/skills/`의 스킬은 **Claude Code 전용**이라 자동으로 로드되지 않는다. 대신 그 내용을
여기서 가리킨다.

## 1. 코드를 만지기 전에 반드시 읽을 것

| 파일 | 무엇이 들었나 |
|---|---|
| `CLAUDE.md` | 작업 규칙(검증 분량·경로 판단)과 **최근 변경 이력 5줄** |
| `.claude/skills/project-context/SKILL.md` | **확정된 설계 결정 전부**(250줄). 코드베이스 탐색을 대신한다 |
| `docs/HISTORY.md` | 과거 변경 이력 전체. "왜 이렇게 돼 있나"의 답이 대개 여기 있다 |

⚠️ **탐색보다 이 세 문서를 먼저 읽는 것이 이 저장소의 규칙이다.** 설계 결정이 주석과 문서에
촘촘히 적혀 있어서, 읽지 않고 고치면 이미 기각된 방향을 되살리게 된다(문서에 "되살리지 말 것"이
명시된 항목이 여럿 있다).

## 2. 명령

```bash
npm ci          # 첫 설치(package-lock.json이 있다)
npm run dev     # 개발 서버 (기본 5173)
npm run build   # 아이콘 서브셋 생성 + tsc -b + vite build
npm test        # vitest run — 현재 691개
```

- `npm run icons`가 `build`/`dev`에 자동으로 선행한다. **없는 아이콘 이름을 쓰면 빌드가 실패한다.**
- `src/icons/generated.ts`는 자동 생성물이지만 **커밋 대상이다**(직접 수정 금지).
- 테스트가 워커 메모리 문제로 죽으면 `npx vitest run --pool=threads --maxWorkers=3`.

## 3. 검증 기준 (CLAUDE.md의 요약)

- 기본은 **`npm test` + `npm run build`**. 이걸로 끝낸다.
- 화면에 보이는 결과가 바뀌는데 테스트로 증명할 수 없는 것(레이아웃 겹침·대비·z-order)은
  **실제 브라우저로 확인**한다. 계산이 아니라 픽셀로 재는 것이 이 저장소의 방식이다.
- 테스트는 **그 변경이 깨뜨릴 수 있는 것만** 덮는다. 한 기능에 수십 개씩 붙이지 않는다.

## 4. 어기면 안 되는 것 (자주 사고가 나는 자리)

1. **게임 수치는 전부 `src/data/`에 있다.** 컴포넌트에 숫자를 적으면 밸런스 테스트가 못 보는
   두 번째 출처가 생긴다.
2. **`src/systems/`는 순수 함수다.** React import 금지, 상태 mutation 금지, `Math.random` 금지
   (무작위가 필요하면 시드를 받는다 — 복권·솔리테어가 그 예다).
3. **없는 CSS 변수를 쓰지 마라.** CSS는 정의되지 않은 `var()`를 만나면 그 선언 전체를 버린다 —
   빌드도 테스트도 통과하고 **화면에서만** 조용히 깨진다. 토큰은 `src/index.css`의 `:root`가
   단일 출처다(`--sp-*`·`--fs-*`·`--os-*`·`--hud-*` 등). ⚠️ `--sp-7`은 없다.
4. **동작하지 않는 컨트롤을 그리지 마라.** 눌러도 아무 일 없는 버튼·갈 데 없는 링크는 만들지
   않는다. 레퍼런스를 베낄 때도 그 자리에 실제 기능을 앉히거나 뺀다.
5. **UI에 이모지를 쓰지 마라.** 아이콘은 Iconify 오프라인 번들만 쓴다(CDN 금지, 외부 이미지 금지).
6. **관계는 한 방향으로만 적는다.** 예: 활동이 요구하는 아이템은 `Activity.requiresItem`에만,
   옷이 맞는 활동은 `ShopItem.outfit.fits`에만 적고 반대쪽은 파생 함수로 찾는다.

## 5. 문서 갱신 의무

설계 결정이 바뀌거나 새 파일·패턴이 생기면 **같은 커밋에서**
`.claude/skills/project-context/SKILL.md`(상한 250줄)와 `CLAUDE.md`의 변경 이력 표를 갱신한다.
이력 표는 최근 5줄만 두고, 넘치는 줄은 `docs/HISTORY.md` 맨 위로 옮긴다.
