# windowsGame

## 하네스: 윈도우 데스크톱 육성 게임 개발

**목표:** Vite + React + TS 육성 게임을 최소 토큰으로 구현·유지보수한다.

**트리거:** 이 게임의 개발 작업(구현/수정/QA/이어하기) 요청 시 `game-pipeline` 스킬을 사용하라. 코드를 만지는 모든 작업 전에 `project-context` 스킬을 먼저 로드하라(코드베이스 탐색 대체). 단순 질문은 직접 응답 가능.

**디자인 규칙:** `ui-ux-pro-max`(685줄 + DB 조회)는 **새 시각 언어를 만들 때만** 로드한다 — 새 화면·새 사이트·팔레트나 타이포 변경. 이미 있는 화면을 고치는 작업은 로드하지 말고 `src/index.css`의 확정된 토큰(`--sp-*`·`--fs-*`·`--el-*`·`--os-*`·`--hud-*`·`--nv-*`)만 쓴다. **토큰에 없는 색·간격·그림자를 새로 만들어야 하면 그 순간 새 시각 언어이므로 스킬을 로드한다** — 이 예외가 규칙의 핵심이다. 감으로 정한 값 금지.

**토큰 규칙:** 파일 3개 이하 수정은 에이전트 스폰 없이 직접 처리. 에이전트 병렬 스폰 최대 2. 에이전트에게 코드 전문 반환 금지(요약만). **에이전트 `model`은 기본 생략(세션 모델 상속)** — 아키텍처·시각 언어 설계·전체 리뷰만 `opus` 명시.

**검증 규칙:** CDP 실측(헤드리스 크롬 실제 클릭)은 **새로 만든 화면에만** 쓴다. 실측이 실제로 잡은 버그는 전부 신규 화면에서 나왔다(게이지 0% 렌더링·잠금화면 대비 미달·메뉴 Esc 먹통). 기존 화면 수정·수치 조정·버그 픽스는 **테스트 + 빌드**로 끝낸다. 다만 **화면에 보이는 결과가 바뀌는데 테스트로 증명할 수 없는 것**(레이아웃 겹침·대비·z-order)은 규모와 무관하게 실측한다.

**검증 분량:** 테스트는 **그 변경이 깨뜨릴 수 있는 것**만 덮는다. 한 기능에 40개씩 붙이지 않는다. 규칙을 뒤집어 실패를 확인하는 증명(이율 반전 등)은 **돈·턴·게임오버를 만드는 불변식에만** 쓴다.

**속도 규칙:** UI 작업은 규모로 경로를 가른다. 파일 3개 이하 + 기존 토큰 안에서 끝나면 **경량 경로**(직접 수정 → 빌드 → 스팟체크). 새 화면·시각 언어 변경·파일 4개 이상만 **풀 경로**. 상세는 `game-pipeline` 스킬.

**변경 이력:** 최근 5행만 이 표에 유지한다. 새 행을 추가해 5행이 넘으면 가장 오래된 행을 `docs/HISTORY.md` 표 맨 위로 옮긴다. 과거 이력 전체는 [docs/HISTORY.md](docs/HISTORY.md).

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-08 | **먼바다투어를 레퍼런스(실제 여행사 홈) 판형으로 다시 짰다** — 헤더+지역 네비 → 연보라 히어로(질문 + 지역 고르기 | 이 달의 상품) → 상품 카드 줄 → 국내 원형 카드 → 고객센터 번호 큰 푸터 | trips(4 → **10종** + `region`·`activityId`·`tripsOf`), activities(**`travel-near`** 신설), TravelSite(tsx+css 전면), trips.test(11건) | 사용자 지시(레퍼런스=실제 여행사 스크린샷). 판형을 채우려 상품을 늘렸고, **지역 축이 생기면서 네비·칩·구역이 전부 진짜 필터가 됐다**. ⚠️ **레퍼런스의 "가격이 내려갔어요"를 그대로 베끼지 않았다** — 이 게임의 값은 활동이 정하므로 내려간 적이 없다(거짓말이 된다). 그 자리에 **지금 소지금으로 갈 수 있는가**라는 진짜 사실을 놓았다(알바몬 히어로 "오늘 최고 일당"과 같은 방식). 판정은 `canRun` 하나다. ⚠️ **예약 검색 폼(목적지·날짜·인원)은 안 만들었다** — 이 게임에 날짜도 인원도 없다. 형태만 빌리고 그 자리에 지역 고르기를 앉혔다. ⚠️ **여행을 둘로 갈랐다**(장거리 250,000 / 근거리 90,000): 근거리는 회복이 절반이지만 **행동력도 덜 먹는다** — 값만 싼 하위 호환이면 하나가 늘 정답이 된다. 상품은 값을 갖지 않고 어느 활동을 실행할지만 가리킨다(배달 메뉴와 같은 규칙). 대비 5.1~15:1 |
| 2026-08-08 | **너튜브 개인방송** — 레일에 [내 채널]이 생기고, 방송 주제 5종 중 하나를 골라 방송을 켠다 | videos(`STREAM_TOPICS`·`subscribersFrom`), TubeSite(tsx 스튜디오 화면·[만들기] 승격, css), sites(너튜브 `activityId: 'stream'`), BrowserApp, sites.test | 사용자 지시. **`stream` 활동은 이미 있었는데 브라우저에서 갈 데가 없었다** — 정의만 있고 스케줄러 예약·바탕화면 바로 가기로만 닿는 활동이었다(방송 장비 340,000원이 여는 유일한 것인데 정작 켤 자리가 없었다). 너튜브가 그 자리다. ⚠️ **주제는 수치를 갖지 않는다** — 켜는 활동은 `stream` 하나이고 주제는 "무엇을 하며 두 시간을 보내는가"만 정한다(증기의 게임 목록·미디북스의 책·시집이의 영화와 같은 규칙). 주제마다 효과를 주면 "번아웃 키는 넷" 같은 불변식이 주제 수만큼 갈라진다. ⚠️ **구독자 수는 `reputation` 파생이고 정산을 안 붙였다** — 트위터 팔로워는 그림 업로드로 늘어나는 **저장된 상태 + 주간 정산**이지만, 방송은 `stream`이 회당 55,000원을 직접 주므로 구독자에까지 수익을 붙이면 **한 행동이 두 번 번다**(트위터는 반대로 게시가 돈을 안 준다). 그래서 새 상태도 새 불변식도 없다. ⚠️ 헤더의 [만들기]를 **표시 전용 `<span>`에서 진짜 버튼으로 승격**했다 — "동작하는 것만 컨트롤로 만든다"는 이 파일의 규칙이고, 이제 갈 데가 생겼다. ⚠️ **실측이 셋을 잡았다**: ①장비 없이 열면 확인창이 실행 버튼을 안 그리고 사유를 적는다("방송용 마이크 세트가 있어야 합니다 — 하이마루에서 구입") — `canRun` 하나가 판정한다는 규칙이 그대로 동작 ②장비를 심고 켜면 1턴이 지나고 평판이 올라 **구독자가 42 → 398명으로 실제로 움직인다**(파생값이 살아 있다) ③패치를 유니코드 이스케이프로 넣다가 **"마쳤습니다"가 "마쿤습니다"로**, "뜬다"가 "뜼다"로 들어갔다(`쳤`를 `쿤`로 썼다) — 빌드·테스트는 통과하고 **화면에서만** 보이는 종류라 실측이 아니면 못 잡는다 |
| 2026-08-08 | **배달의정석(배달 음식) 신설 + 쇼핑 띠에 배달 탭** — 메뉴 8종, 정크푸드는 **매력 -2** | dishes(신규 data + test 10건), FoodSite(tsx+css 신규), activities(`meal-junk`·`meal-healthy`), sites(`render: 'food'` + `STORE_SITES` 확장), BrowserApp, sites.test | 사용자 지시. **알바몬과 같은 구조다**: 메뉴는 어느 활동을 실행할지 가리키기만 하고 값은 활동이 갖는다 — 그래서 카드의 가격은 `previewActivity`가 돌려준 money 행에서 파생되고 같은 부류 메뉴는 값이 같다(같은 직종 공고가 같은 일당인 것과 같은 이유). ⚠️ **행동력을 회복시키지 않는다** — 활동으로 `stamina`를 채우면 "턴을 써서 행동력을 얻는" 순환이 생기고 "취침만 회복한다"는 자원 규칙이 무너진다. 음식이 주는 것은 멘탈이고 몸에 남는 것은 건강식의 체력 그릇뿐이다. ⚠️ **정크푸드의 매력 감소를 카드에서 숨기지 않았다** — 확인창까지 가야 알면 기습이 된다(알바몬이 잠금 사유를 목록에 적는 것과 같은 방향). 폭이 -2인 것은 크게 깎으면 "싸게 멘탈 채우는 길"이 아니라 함정이 되기 때문이다. ⚠️ **`systems/delivery.ts`(택배)와 이름이 겹치지 않게 `render: 'food'`다** — 음식은 인벤토리에 안 쌓이고 다음 날 도착하지도 않는다. ⚠️ 색은 DB `Restaurant/Food Service`의 **빨강**이다: 같은 DB의 배달 주황(#EA580C)을 쓰면 하이마루(#C2410C)와 같은 인상이 된다 |
| 2026-08-08 | **노24를 레퍼런스(실제 티켓 예매 사이트 홈) 판형으로 다시 짰다** — 검은 헤더 + 분류 네비 → 큰 캐러셀 + 썸네일 줄 → WHAT'S HOT(큰 1 + 3×2) → 분류 구역 → 전화번호 큰 푸터 | shows(공연 6 → **12종** + `HERO_SHOWS`·`HOT_SHOWS`), TicketSite(tsx+css 전면) | 사용자 지시(레퍼런스=실제 티켓 사이트 스크린샷). **판형을 채우려면 콘텐츠가 먼저**여서 공연을 배로 늘렸다 — 6종으로 캐러셀·HOT·분류 구역을 만들면 같은 공연이 세 번 나온다. ⚠️ **편성은 데이터가 정한다**(`SHOWS` 배열 순서 = 앞 넷 캐러셀 / 앞 일곱 HOT) — 화면이 "무엇을 크게 걸까"를 스스로 고르지 않는다(`DESKTOP_ITEMS`와 같은 규칙). ⚠️ **레퍼런스에서 덜어낸 것**: 로그인·장바구니(계정이 없다), [더보기](더 보여 줄 목록이 없다), TICKET OPEN의 D-3 뱃지(오픈일을 데이터로 만들면 게임 날짜와 이어야 하고 그 순간 새 정산 규칙이 생긴다), 광고 배너 격자·FOCUS ON 영상(광고는 포털 배너존 하나뿐이고 영상은 아예 없다). ⚠️ **헤더만 검다** — DB `Music Streaming`의 어두운 배경을 통째로 쓰면 브라우저 창 안에서 이질적으로 뜬다(시집이의 어두운 극장 톤이 기각된 전례). ⚠️ 실측: WHAT'S HOT 격자를 `auto-fit`으로 두니 여섯이 **한 줄로** 늘어서 큰 카드와 높이가 어긋났다 — **3열 고정**으로 바꿨다. 대비 6.4~16.9:1 |
| 2026-08-08 | **네이놈증권(주식) 신설** — 네이놈은행 옆 카드. 종목 4종을 사고팔고, 시세는 지난 14일 스파크라인으로 본다 | stocks(신규 data+systems+test 17건), StockSite(tsx+css 신규), sites(`render: 'stock'` + 항목), types(`StockState`·`StockTrade`), gameStore(`buyStock`/`sellStock`·`reviveStocks`), events(`first-stock`), BrowserApp | 사용자 지시("네이놈은행 옆에"). **은행과 같은 부류이되 성격을 정반대로 갈랐다** — 은행은 참으면 확실히 조금 붙고, 증권은 맞히면 빨리 붙고 틀리면 줄어든다(둘이 같으면 하나가 남는다). 은행·부동산처럼 **활동을 실행하지 않고 턴을 안 쓴다**. ⚠️ **밤 정산이 없다**(즉시 체결) — `nightPayoutPending`에 원천을 더하지 않은 유일한 돈 시스템이다. ⚠️ **시세를 저장하지 않는다: 날짜의 순수 함수다.** 저장하면 새로 고칠 때마다 다시 굴러 **세이브 스커밍**이 열린다(복권이 일련번호를 시드로 쓰는 것과 같은 이유). 파형은 **정현파 셋 + 작은 잡음**이다 — 난수만 쓰면 톱니라 주가로 안 읽히고, 정현파만 쓰면 며칠 만에 주기를 외운다. ⚠️ **화면은 미래를 한 칸도 안 그린다**(`chartOf`가 오늘까지 자른다) — 순수 함수라 내일 값도 계산되지만 그리는 순간 버튼 하나짜리 무한 수익이 된다. ⚠️ **`maxShares`가 "판은 반드시 끝난다"를 지탱한다**: 완벽한 타이밍으로 매매해도 하루 최대 38,308원 < 가장 싼 집 마지막 물가 생활비 45,600원(여유 7,292원)이고, `stocks.test.ts`가 데이터에서 직접 계산해 **규칙을 뒤집는 증명까지** 붙였다 — 상한이 없으면 소지금이 늘수록 포지션이 커져 복리로 폭발한다. ⚠️ **상승 빨강·하락 파랑**(한국 증시 관행)인데 이 앱에서 빨강은 위험이라 뜻이 부딪힌다: **▲▼ + 부호 + %가 뜻을 지고 색은 거들기만 한다**(ux `color-not-only`), 0에는 색도 부호도 안 준다. 색은 슬레이트 #334155(DB `Inventory & Stock Management`) — 파랑 셋·청록이 이미 차서 채도 없는 계열로 갈랐다. ⚠️ **실측이 둘을 잡았다**: ①`npm run build`가 **아이콘 이름에서 막혀 있었다**(`beaker-24`는 fluent-color에 없다 — `tsc`만 돌려 보고 통과로 착각했다. CDP가 옛 dist를 보고 있었다) ②1일차에 **등락이 전부 "보합"이고 차트가 점 하나**였다 — 판 시작 전 날짜를 잘라 낸 클램프 탓이고, 회사는 게임보다 먼저 있었으므로 그 클램프가 틀렸다(음수 날도 정상 계산하도록 고쳤다) |

